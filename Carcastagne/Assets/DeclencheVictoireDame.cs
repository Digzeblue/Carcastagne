using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeclencheVictoireDame : MonoBehaviour
{
    // Start is called before the first frame update

    public GameObject boutton;
    public GameObject EcranVictoire;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*
    void OnTriggerEnter2D(Collision2D Joueur)
    {
        if (Joueur.gameObject.tag == "Cochon")
        {
            
        }
    }
    */
}
